# Sensor-set ablation config (harness.sensor_matrix): odom-depth
rig: {source: bag}
sensors:
  odometry: [{topic: /odom}]
  depth:
    - topic: /d400/aligned_depth_to_color/image_raw
      every_nth: 3
